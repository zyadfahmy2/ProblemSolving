#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int maxProfit(int[] prices)
    {
        int n = sizeof(prices) / sizeof(*prices);
        for (int i = 0; i < ; i++)
        {
        }
    }
}